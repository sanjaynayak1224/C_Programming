// AreaOfCircle
#include <stdio.h>
int main()
{
    float radius;
    printf("Enter The Radius :");
    scanf("%f", &radius);
    printf("The Area Of The Circle is %f", radius * radius);
    return 0;
}